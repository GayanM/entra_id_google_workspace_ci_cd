name: CI/CD for Google Workspace App

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      GH_PAT: ${{ secrets.GH_PAT }}

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      - name: 🔄 Export Google Workspace App Config
        shell: pwsh
        run: ./scripts/export_google_workspace_app.ps1

      - name: 🧼 Configure Git
        shell: pwsh
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

      - name: 📦 Commit and push exported config
        shell: pwsh
        run: |
          git add config/
          git commit -m "📦 Auto-exported Google Workspace app config"
          git push https://x-access-token:${env:GH_PAT}@github.com/${{ github.repository }} HEAD:main

      - name: 🚀 Import App Configuration
        shell: pwsh
        run: ./scripts/import_google_workspace_app.ps1

      - name: 👥 Assign Users
        shell: pwsh
        run: ./scripts/assign_users.ps1
